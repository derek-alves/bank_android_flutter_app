name: micro_app_profile
description: "A new Flutter package project."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  micro_core:
    path: ../../micro_core/

  event_bus:
    path: ../../common/event_bus/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
